-- lists all records of table second_table in databse hbtn_0c_0
-- displays both the score and name, in this order
-- records should be ordered by score, top first

SELECT score, name FROM second_table ORDER BY score DESC;
